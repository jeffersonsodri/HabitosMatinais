const axios = require('axios');
const Act = require('../models/Activity');


module.exports = {

    async store (req, res) {

        return res.json({ok: true});
    },

    async index (req, res) {
        
        return res.json({ok: true});
    }

};
